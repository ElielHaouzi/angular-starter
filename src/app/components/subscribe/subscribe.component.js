import template from './subscribe.pug';
import controller from './subscribe.controller';
import './subscribe.scss';

let SubscribeComponent = {
  restrict: 'E',
  bindings: {},
  template: template(),
  controller
};

export default SubscribeComponent;
